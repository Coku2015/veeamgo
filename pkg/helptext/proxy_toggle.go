package helptext

const (
	ProxyEnableShort = "Enable a backup proxy"
	ProxyEnableLong  = "Enables a disabled backup proxy so it can process new tasks."

	ProxyDisableShort = "Disable a backup proxy"
	ProxyDisableLong  = "Disables a backup proxy to prevent it from accepting new tasks."
)
