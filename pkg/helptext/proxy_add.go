package helptext

const (
    ProxyAddShort = "Add a backup proxy"
    ProxyAddLong  = "Registers a backup proxy using an existing managed server as the host. Supported proxy platforms are vmware, hyperv, and general (mapped to the VBR API types ViProxy, HvProxy, and GeneralPurposeProxy)."
)
